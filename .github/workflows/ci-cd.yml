name: "Create Feature Branch"

on:
  workflow_dispatch:

jobs:
  create-branch:
    runs-on: ubuntu-latest
    steps:
    - name: Check out repository
      uses: actions/checkout@v4
   # - name: Identifiy branch
   #   id: extract_branch2
   #   run: |
   #     echo "/Repos/Vitesse/${{ github.ref }}"
   #     echo "/Repos/Vitesse/${{ github.ref_name }}"
   #     echo "branch=${{ github.ref_name }}" >> $GITHUB_OUTPUT
    - id: foo
      uses: ./.github/actions/github-branch-name
    - run:  echo ${{ steps.foo.outputs.branch }}
      shell: bash
    - run: echo random-number ${{ steps.foo.outputs.random-number }}
      shell: bash
    - run: echo random-number ${{ steps.foo.outputs.branch }}
      shell: bash
